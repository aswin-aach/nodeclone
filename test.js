
var name = "emacs"

setTimeout(() => console.log(`${name} 1s`), 1000)

setTimeout(() => console.log(`${name} 2s`), 2000)

setTimeout(() => console.log(`${name} 3s`), 3000)

console.log(`${name} immediate`)
